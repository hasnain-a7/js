// stack and heap meomry

// primitive data type are (Stack)
// non-primitive data type are (Heap)

// it means if you change value of stack memoary's variable you can only change
// value of is copy
// if you change the value of heap memoary's variable so you can change original value
// of it not it's copy